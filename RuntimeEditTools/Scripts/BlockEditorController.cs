using UnityEngine.Events;

namespace RuntimeEditTools
{
    class BlockEditorController
    {
        //public BlockEditorController(
        //    IButton nameInputButton,
        //    IButton tagInputButton,
        //    IButton formFactorInputButton,
        //    IChangeText nameText,
        //    IChangeText tagText,
        //    IChangeText formFactorText,
        //    IEnumerable<IKeyboard> keyboards)
        //{
        //    InputFieldLinker inputFieldLinker = new();
        //    foreach (var keyboard in keyboards) {
        //        inputFieldLinker.AddKeyboard( keyboard );
        //    }

        //    nameInputButton.onClick += (_) => {
        //        inputFieldLinker.Link( nameText );
        //    };
        //    tagInputButton.onClick += (_) => {
        //        inputFieldLinker.Link( tagText );
        //    };
        //    formFactorInputButton.onClick += (_) => {
        //        inputFieldLinker.Link( formFactorText );
        //    };

        //}
    }
}
